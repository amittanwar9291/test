Given - I am logged in
And I am on the Page 8 of Cranial MRI
And I select 'Finding' radio button
And I select 'Brainstem' from the dropdown
And 'I describe' button is selected

When I select 'I know' button 
Then 'Differential diagnosis 1*' dropdown is enabled 

And 'Mesencephalon' checkbox is enabled 
And 'Pon' checkbox is enabled 
And 'Medulla oblongata' checkbox is enabled 

When I select 'Acute stroke' from 'Differential diagnosis 1*' dropdown
Then 'Histology known' checkbox is enabled

When I select 'Histology known' checkbox
Then 'Benign' radio button is enabled
And 'Malignant' radio button is enabled 

When I select 'Benign' radio button
Then radio button is selected
When I select 'Malignant' radio button
Then radio button is selected

When I select 'Characterization II' tab
Then 'Substance defect' checkbox is enabled 
And 'Hemorrhage' checkbox is enabled 
And 'Necrosis' checkbox is enabled 
And 'Calcification' checkbox is enabled 
And 'Fat' checkbox is enabled 
And 'Cysts' checkbox is enabled 
And 'Capsule' checkbox is enabled 
And 'Septa' checkbox is enabled 
And 'Signal void' checkbox is enabled 
And 'Fluid Level' checkbox is enabled  

And 'Confluent' checkbox is enabled 
And 'Mass effect' checkbox is enabled 
And 'Infiltration' checkbox is enabled 
And 'Perineural tumor spread' checkbox is enabled  

And 'Hydrocephalus' checkbox is enabled 
And 'Perifocal edema' checkbox is enabled 
And 'Midbrain atrophy' checkbox is enabled 
And 'Brainstem atrophy' checkbox is enabled 
And 'Nerve thickening' checkbox is enabled 
And 'Nerve compression' checkbox is enabled 

And 'No' radio button is enabled 
And 'Low' radio button is enabled 
And 'Strong' radio button is enabled 

When I select 'Substance defect' checkbox
Then checkbox is selected 
When I select 'Hemorrhage' checkbox
Then checkbox is selected 
When I select 'Necrosis' checkbox
Then checkbox is selected 
When I select 'Calcification' checkbox
Then checkbox is selected 
When I select 'Fat' checkbox
Then checkbox is selected 
When I select 'Cysts' checkbox
Then checkbox is selected 
When I select 'Capsule' checkbox
Then checkbox is selected 
When I select 'Septa' checkbox
Then checkbox is selected 
When I select 'Signal void' checkbox
Then checkbox is selected 
When I select 'Fluid Level' checkbox
Then checkbox is selected 

When I select 'Confluent' checkbox
Then checkbox is selected 
When I select 'Septa' checkbox
Then checkbox is selected 
When I select 'Mass effect' checkbox
Then checkbox is selected 
When I select 'Infiltration' checkbox
Then checkbox is selected 
When I select 'Perineural tumor spread' checkbox
Then checkbox is selected 

When I select 'Hydrocephalus' checkbox
Then checkbox is selected 
When I select 'Perifocal edema' checkbox
Then checkbox is selected 
When I select 'Midbrain atrophy' checkbox
Then checkbox is selected 
When I select 'Brainstem atrophy' checkbox
Then checkbox is selected 
When I select 'Nerve thickening' checkbox
Then checkbox is selected 
When I select 'Nerve compression' checkbox
Then checkbox is selected 

When I select 'No' radio button 
Then radio button is selected

When I select 'Low' radio button 
Then 'Homogeneous' radio button is enabled
And 'Heterogeneous' radio button is enabled

And 'Peripheral' radio button is enabled
And 'Rim enhancement' radio button is enabled
And 'Entire lesion' radio button is enabled
And 'Central' radio button is enabled
And 'Septal' radio button is enabled

When I select 'Homogeneous' radio button 
Then radio button is selected
When I select 'Heterogeneous' radio button 
Then radio button is selected

When I select 'Peripheral' radio button 
Then radio button is selected
When I select 'Rim enhancement' radio button 
Then radio button is selected
When I select 'Entire lesion' radio button 
Then radio button is selected
When I select 'Central' radio button 
Then radio button is selected
When I select 'Septal' radio button 
Then radio button is selected

When I select 'Low' radio button 
Then 'Homogeneous' radio button is enabled
And 'Heterogeneous' radio button is enabled

And 'Peripheral' radio button is enabled
And 'Rim enhancement' radio button is enabled
And 'Entire lesion' radio button is enabled
And 'Central' radio button is enabled
And 'Septal' radio button is enabled

When I select 'Homogeneous' radio button 
Then radio button is selected
When I select 'Heterogeneous' radio button 
Then radio button is selected

When I select 'Peripheral' radio button 
Then radio button is selected
When I select 'Rim enhancement' radio button 
Then radio button is selected
When I select 'Entire lesion' radio button 
Then radio button is selected
When I select 'Central' radio button 
Then radio button is selected
When I select 'Septal' radio button 
Then radio button is selected


When I select 'Low' radio button 
Then 'Homogeneous' radio button is enabled
And 'Heterogeneous' radio button is enabled

And 'Peripheral' radio button is enabled
And 'Rim enhancement' radio button is enabled
And 'Entire lesion' radio button is enabled
And 'Central' radio button is enabled
And 'Septal' radio button is enabled

When I select 'Homogeneous' radio button 
Then radio button is selected
When I select 'Heterogeneous' radio button 
Then radio button is selected

When I select 'Peripheral' radio button 
Then radio button is selected
When I select 'Rim enhancement' radio button 
Then radio button is selected
When I select 'Entire lesion' radio button 
Then radio button is selected
When I select 'Central' radio button 
Then radio button is selected
When I select 'Septal' radio button 
Then radio button is selected


When I select 'Strong' radio button 
Then 'Homogeneous' radio button is enabled
And 'Heterogeneous' radio button is enabled

And 'Peripheral' radio button is enabled
And 'Rim enhancement' radio button is enabled
And 'Entire lesion' radio button is enabled
And 'Central' radio button is enabled
And 'Septal' radio button is enabled

When I select 'Homogeneous' radio button 
Then radio button is selected
When I select 'Heterogeneous' radio button 
Then radio button is selected

When I select 'Peripheral' radio button 
Then radio button is selected
When I select 'Rim enhancement' radio button 
Then radio button is selected
When I select 'Entire lesion' radio button 
Then radio button is selected
When I select 'Central' radio button 
Then radio button is selected
When I select 'Septal' radio button 
Then radio button is selected



