Given	I'm logged-in with valid credentials
And I select 'Yes' radio button from contrast enhanced box in Page 03
And	I'm on Page 04 of Hand MRI
When I select 'Finding' radio button
And I select 'Mass/Osteolysis' from the dropdown menu
And I select 'I describe' or 'I know' radio button

When I select 'Characterization I' tab is enabled
Then 'Sharp' radio button is enabled
And 'Ill-defined' radio button is enabled
And 'Mixed form' radio button is enabled

And 'Homogeneous' radio button is enabled
And 'Heterogeneous' radio button is enabled

And 'Intact' radio button is enabled
And 'Thinning ("scalopping")' radio button is enabled
And 'Destruction' radio button is enabled
And 'Neocorticalis' radio button is enabled

And 'T1w signal' slidebar is enabled
And 'T2w signal' slidebar is enabled

And 'No' radio button is enabled
And 'Weak' radio button is enabled
And 'Strong' radio button is enabled

And 'Round' checkbox is enabled
And 'Lobulated' checkbox is enabled
And 'Geographic' checkbox is enabled
And 'Sessile' checkbox is enabled
And 'Pedunculated' checkbox is enabled
And 'Orientation: from the epiphysis toward the diaphysis' checkbox is enabled

When I select 'Weak' radio button
Then 'Homogeneous' radio button is enabled
And 'Heterogeneous' radio button is enabled
And 'Peripheral' radio button is enabled
And 'Central' radio button is enabled
And 'Complete lesion' radio button is enabled
And 'Septal' radio button is enabled
And 'Within the nidus' radio button is enabled

When I select 'Strong' radio button
Then 'Homogeneous' radio button is enabled
And 'Heterogeneous' radio button is enabled
And 'Peripheral' radio button is enabled
And 'Central' radio button is enabled
And 'Complete lesion' radio button is enabled
And 'Septal' radio button is enabled
And 'Within the nidus' radio button is enabled
