Then I select any Examination radio button from Page 01 of Pelvic MRI
And I go to Page 3 and select 'Yes' in Contrast enhanced
And I go to Page 05 of Pelvic MRI
And I click on 'Finding' radio button

When I select 'Scrotum, infection/inflammation' from the dropdown
Then 'Add new finding' button is enabled
And 'Testicle/Epididymis' tab is preselected

And 'T2w signal' below 'Testicles' is enabled
And 'Strong' radio button is enabled
And 'Low' radio button is enabled
And 'No' radio button is enabled

When I drag 'T2w signal' below 'Testicles' to the last position
Then it stops at 'below 'Hyperintense'

And 'T2w signal' below 'Epididymis' is enabled
And 'Strong' radio button is enabled
And 'Low' radio button is enabled
And 'No' radio button is enabled

When I drag 'T2w signal' below 'Epididymis' to the last position
Then it stops at 'below 'Hyperintense'

When I select 'Strong' radio button in the 2nd column
Then 'Homogeneous' checkbox is enabled
And 'Heterogeneous' checkbox is enabled

When I select 'Low' radio button in the 2nd column
Then 'Homogeneous' checkbox is enabled
And 'Heterogeneous' checkbox is enabled

When I select 'Strong' radio button in the 3rd column
Then 'Homogeneous' checkbox is enabled
And 'Heterogeneous' checkbox is enabled

When I select 'Low' radio button in the 3rd column
Then 'Homogeneous' checkbox is enabled
And 'Heterogeneous' checkbox is enabled
