Given I'm logged-in with valid credentials
When I select 'Female' radio button from Page 01 of Pelvic MRI
And I select any Examination radio button from Page 01 of Pelvic MRI
Then I go to Page 03 and select 'Yes' in Contrast enhanced
Then I select 'Sequences' box
And select 'DWI' from the Weighting' dropdown
Then I go to Page 06 of Pelvic MRI
And I click on 'Finding' radio button
And dropdown below 'Finding' is enabled

When I select 'Vagina mass' from the dropdown
Then 'Add new finding' button is enabled
And 'Characterization I' tab is preselected
And 'I describe' button is preselected

When I select 'Characterization II' tab
Then 'Fat' checkbox is enabled
And 'Cysts' checkbox is enabled
And 'Calcification' checkbox is enabled
And  'Capsule' checkbox is enabled
And  'Septa' checkbox is enabled
And  'Necrosis' checkbox is enabled
And  'Signal voids' checkbox is enabled
And  'Hemorrhage' checkbox is enabled
And  'Possibly superinfected/necrotic areas' checkbox is enabled
And  'Focal signal voids' checkbox is enabled

And 'Strong' radio button is enabled
And 'Low' radio button is enabled
And 'No' radio button is enabled

And  'Diffusion restriction' checkbox is enabled

And  'Fistula' checkbox is enabled
And  'Hydroureter' checkbox is enabled
And  'Peritoneal nodules' checkbox is enabled
And  'Ascites' checkbox is enabled

And 'Paravaginal soft tissue' checkbox is enabled
And 'Cervix uteri' checkbox is enabled
And 'Parametria' checkbox is enabled
And  'Pelvic wall' checkbox is enabled
And  'Ureter' checkbox is enabled
And  'Urinary bladder' checkbox is enabled
And  'Rectum' checkbox is enabled
And  'Peritoneum' checkbox is enabled

When I select 'Strong' radio button
Then 'Homogeneous' radio button is enabled
And 'Heterogeneous' radio button is enabled

When I select 'Low' radio button
Then 'Homogeneous' radio button is enabled
And 'Heterogeneous' radio button is enabled

When I select 'Fistula' radio button
Then 'Vesicovaginal' radio button is enabled
And 'Rectovaginal' radio button is enabled

When I select 'Peritoneal nodules' radio button
Then 'Unifoca' radio button is enabled
And 'Multifocal' radio button is enabled
And 'Rectouterine pouch' radio button is enabled

When I select 'Ascites' radio button
Then 'T1w signal increase' radio button is enabled
And 'Hemosiderin' radio button is enabled
And 'Focal' radio button is enabled
And 'Generalized' radio button is enabled

