export enum SequencesTypeEnum { // TODO should also create some ProtocolsTypeEnum alias with the same values to be used in CT modules
  None = 'None',
  Auto = 'Auto',
  CenterStandard = 'CenterStandard',
  Individual = 'Individual'
}
